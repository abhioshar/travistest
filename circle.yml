machine:
   node:
     version: 7
 
dependencies:
  override:
    - npm install -g @angular/cli@latest:
      pwd:  angular-src
    - npm install:
      pwd: angular-src
 
test:
  override:
    - ng test:
      pwd: angular-src