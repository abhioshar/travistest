machine:
   node:
     version: 7
 
dependencies:
  override:
    - npm install -g @angular/cli:
      pwd:  angular-src
    - npm install:
      pwd: angular-src
 
test:
  override:
    - ng test:
      pwd: angular-src