general:
  build_dir: angular-src

machine:
   node:
     version: 7

dependencies:
  override:
    - pwd && ls && npm install -g @angular/cli@latest && pwd && ls
    - pwd && ls && npm install && pwd && ls
 
test:
  override:
    - pwd && ls && ng test && pwd && ls