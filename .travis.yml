language: node_js
node_js: 
  - "7"

branches:
  only:
    - master

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

before_install:
  - cd angular-src

install:
  - npm install -g @angular/cli
  - npm install

script:
  - pwd
  - ls
  - npm test