language: node_js
node_js: 
  - "7"

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - cd angular-src
  - npm install

before_install:
  - cd angular-src

script:
  - ng test --watch=false